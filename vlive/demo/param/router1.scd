(

~routingMatrix = (
	new: { arg self, sources, effects;
		self = self.deepCopy;
	
		
		self.sources = sources;
		self.effects = effects;
	
		self;
	},

	route: { arg self, in_idx, out_idx;

		self.sour
		
	},
);


)
NdefMixer.new
Pdef(\plop).set(\out, NdefMixerChannel(\hey).inBus)
NdefMixerChannel(\hey);
N
ProxyPreset.new

ProxyChain(\hey).gui
ProxyChain(\hey).gui



