
//Veco(~name.debug("name")).uname.debug("heyyy");
(
Ndef(~name+++\fx).put(50, \filter -> { arg in;
	var sig = in;
	
	sig = sig * SinOsc.ar(120);

	sig;
});
);

// END

Veco.main.name
Veco('stone12-64')
Veco.side_project[]
~trigpadboard[\veco].

Veco.previous_activated_project[\veco].project_name
Veco.last_activated_project[\veco].project_name
