
"what the fuckXX".debug;
~timeline_do.(2);

"what the fuck".debug;

// END

~spawn_cliprow.(~index,~name)
Veco(~name).lib
~spawnclip.(1, ~index)
~spawnclip.(1, 14)

(
	
	switch(nil,
		nil, {
			"hello".debug;
		},
		15, {
			"hi".debug;
		},
		{
			"nope".debug;
		}
	
	)
)
