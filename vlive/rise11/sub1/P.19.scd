
"what the fuckXX".debug;
~timeline_do.(8, { { 0.5.wait; Ndef(Veco(0).uname+++\fx).stop }.fork });

"what the fuck".debug;

// END

~spawn_cliprow.(~index,~name)
Veco(~name).lib
~spawnclip.(1, ~index)
~spawnclip.(1, 14)

(
	
	switch(nil,
		nil, {
			"hello".debug;
		},
		15, {
			"hi".debug;
		},
		{
			"nope".debug;
		}
	
	)
)
